version: '3.4'

services:
  customerapi:
    image: ${DOCKER_REGISTRY-}customerapi
    build:
      context: .
      dockerfile: CustomerAPI/Dockerfile

  onefin:
    image: ${DOCKER_REGISTRY-}onefin
    build:
      context: .
      dockerfile: Dockerfile

